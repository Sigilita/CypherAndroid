<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nasser.cypher"
    android:versionCode="1"
    android:versionName="1.0" >
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="15" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Login"
            android:label="@string/title_activity_login" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Submenu"
            android:label="Sub Menu"
            ></activity>
        <activity
            android:name=".Encriptar"
            android:label="Encriptar"
            ></activity>
        <activity
            android:name=".MenuDesencriptar"
            android:label="Menu Desencriptar"
            ></activity>
         <activity
            android:name=".Desencriptar"
            android:label="Desencriptar"
            ></activity>
        
    </application>
    

</manifest>